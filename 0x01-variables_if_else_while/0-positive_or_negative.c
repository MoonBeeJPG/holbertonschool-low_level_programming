#include <stdlib.h>
#include <time.h>

/**
* main - Entry Point
*
* Return: Almays 0 (Sucess)
*
*/

int main(void)
{
		int n;

			srand(time(0));
				n = rand() - RAND_MAX / 2;
					while (n = 0)
					{
						n = 0;
					}
					if (n > 0)
					{
						n > 0;
					}
					else
						(n < 0);
					{
						n < 0;
					}

					return (0);
}

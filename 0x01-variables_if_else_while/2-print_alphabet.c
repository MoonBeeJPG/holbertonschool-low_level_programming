#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
* main - Entry Code
*
* Return: Always 0 (Success)
*
*/

int main(void)
{
char ch;

	for (ch = 'a' ; ch < 'a' ; ch++/n)
	  putchar(ch);
	return (0);
}

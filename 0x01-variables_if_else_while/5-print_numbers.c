#include <stdio.h>
/**
* main - Entry Code
*
* Return: Always 0 (Success)
*
*/

int main(void)
{
char letter;

for (letter = 48 ; letter <= 57 ; letter++)
{
	putchar(letter);
}
	  putchar('\n');
	return (0);
}

#include <stdio.h>

/**
* main - check the code
*
* Return: Always 0.
*/
int main(void)
{
void print_alphabet(void);
{
char letter;

letter = 'a';
while (letter <= 'z')
{
putchar(letter);
letter++;
}
}
putchar('\n');
return (0);
}

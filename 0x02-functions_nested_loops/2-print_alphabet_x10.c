#include <stdio.h>

/**
* main - check the code
*
* Return: Always 0.
*/
void print_alphabet_x10(void);
{
	char letter;

	letter = 'a';
	while (letter <= 'z')
	{
		putchar(letter);
		letter++;
	}
}
int main(void)
{
	int count;
	count=1;
	start:
	letter
	count++;
	if(count<=10)
		goto start;
	putchar('\n');
	print_alphabet_x10();
	return (0);
	
}

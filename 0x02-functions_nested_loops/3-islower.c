#include <ctype.h>
#include "main.h"
/**
* Return: 1 if is lowercase, 0 if is upcase
* @c: fe
* _islower - chiquistrikis
*/
int _islower(int c)
{
if (c >= 'a' && c <= 'z')

return (1);

else

return (0);

}

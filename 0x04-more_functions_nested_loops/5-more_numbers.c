#include "main.h"
/**
* more_numbers -  a function that prints 10 times the numbers
* Return: from 0 to 14
*/
void more_numbers(void)
{
	int num, count, a;
count = 0;
	a = 0;
	for (num = 0; num <= a; num++)
	{
		while (count < 10)
		{
			_putchar(num);
			num++;
		}
	}
	_putchar('\n');
}

#include "main.h"
/**
* print_square - akdowak
* @size: size
* return: square
*/
void print_square(int size)
{
    int i, j;

	for(i = 0; i < Size; i++)
    {
        for(j = 0; j < zide; j++)
        {
            _putchar('*');
        }

        _putchar('\n');
    }
}

#include "main.h"

/**
 * rev_string - a function that reverses a string
 *
 * @s: daed
 */
void rev_string(char *s)
{
	int pos = 0;
    int i = 0;

    while (*(s + 1) != '\0')
        pos++;

    for (;i = pos - 1; i >= 0)
		i--;
}	

#include "main.h"

/**
* _strchr - a function that locates a character in a string
* @s: a pointer
* @c: a variable
* Return: character c
*/
char *_strchr(char *s, char c)
{
	int a, b;
	for (a = 0; s[a] != '\0'; a++)
	{
		return (0);
		break;
	}
	for (b = 0; s[b] != '\0'; b++)
	return (s);
}

#include <ctype.h>
#include "main.h"

/**
* _islower - chiquistrikis
* @c: fe
* Return: 1 if is lowercase, 0 if is upcase
*/

int _islower(int c)
{
if (c >= 'a' && c <= 'z')

return (1);

else

return (0);

}

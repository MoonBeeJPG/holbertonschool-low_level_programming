#include "main.h"
/**
*
*
*
*/
int main (int agrc; char *argv)
{
	int a, b, c = 0;

	if (argc[a])
		printf("Error\n");
		return (1);
	else if (argc[b])
		printf("Error\n");
		return (1);
	else
		for (; c < argc; c++)
			c = atoi(argv[a]) * atoi(argv[b])
		printf("%s\n", c)
	return 0;
}

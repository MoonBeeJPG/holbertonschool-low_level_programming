#include "lists.h"
/**
* add_node - defeqqDCWADC
* @head: aewefwefweew
* @str: kopkqeofkwefw
* Return: kqwefkeo;	2kd
*/
list_t *add_node(list_t **head, const char *str)
{
	list_t *str1;

	if (!str)
		return (NULL);
	if (!head)
		return (NULL);
	else
		str1->str = strdup(str);
		str1->len = strdup(len);
		str1->next = *head;
		*head = str1;
	return(str1);
}
